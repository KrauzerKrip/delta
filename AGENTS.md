# Repository Layout

Runtime code lives under `Code`, scenes and prefabs under `Assets`, third-party or reusable packages under `Libraries`, and MSTest coverage under `UnitTests`.

## Working Rules

- Preserve CRLF line endings in every text file you create or modify.
- Before completing a task, verify that all affected text files contain no lone LF line endings.
